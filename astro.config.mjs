// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://pssjym.github.io',
  base: '/protein-young-web', // リポジトリ名を / から書き始めます
});
